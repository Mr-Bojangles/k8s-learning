apiVersion: v1
kind: Service
metadata:
  name: crawler-service
  namespace: crawler
spec:
  selector:
    app: synergychat-crawler
  type: ClusterIP # Optional, k8s default
  ports:
    - protocol: TCP
      port: 80 # External facing port Service listens on
      targetPort: 8080 # Internal port Pods are listening on
